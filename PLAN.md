# CUA Remaining Production Plan

- Prove the GPUI dynamic-island overlay with a host-visible capture path.
- Add shared-session arbitration with owner, observer, lease, cancellation, and kill-switch scopes.
- Expand runtime inventory with daemon PID, bound HTTP address, profile socket, connected clients, HUD process, and active session metadata.
